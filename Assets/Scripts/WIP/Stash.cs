using UnityEngine;
using System.Collections;

public class NewMonoBehaviour : MonoBehaviour
{
    /// <summary>
    /// Tried to auto-break cutscene text
    /// </summary>
    //TextBlocks.ForEach(block =>
    //    {
    //        string formattedText = string.Join(
    //            " ", block.Text.Split("\r\n")).Replace("  ", " ");
    //Debug.Log($"1: {formattedText}");

    //        var index = 0;
    //formattedText.ToList().ForEach(c =>
    //        {
    //    index++;
    //    if (index == 90)
    //        formattedText = formattedText.Insert(90, "\r\n");
    //    if (index == 60)
    //        formattedText = formattedText.Insert(60, "\r\n");
    //    if (index == 30)
    //        formattedText = formattedText.Insert(30, "\r\n");
    //});
    //            Debug.Log($"2: {formattedText}");
    //        block.Text = formattedText;
    //    });

}
